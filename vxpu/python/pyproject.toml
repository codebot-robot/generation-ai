[project]
name = "vxpu"
version = "0.1.0"
description = "Run PyTorch models on remote accelerators via portable artifacts"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "grpcio",
    "protobuf>=6.30",
    "requests",
    "huggingface_hub",
]

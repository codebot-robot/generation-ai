---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: chatsessionmessages.bema.labs.gke.io
spec:
  group: bema.labs.gke.io
  names:
    kind: ChatSessionMessage
    listKind: ChatSessionMessageList
    plural: chatsessionmessages
    singular: chatsessionmessage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChatSessionMessage is the Schema for the chatsessionmessages
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChatSessionMessageSpec defines the desired state of ChatSessionMessage
            properties:
              parts:
                description: Parts is the content of the message.
                items:
                  description: Part represents a part of a message.
                  properties:
                    functionRequest:
                      description: FunctionRequest is a request to call a function.
                      properties:
                        args:
                          description: Args are the arguments to the function call.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        name:
                          description: Name is the name of the function to call.
                          type: string
                      required:
                      - name
                      type: object
                    functionResponse:
                      description: FunctionResponse is the response from a function
                        call.
                      properties:
                        name:
                          description: Name is the name of the function that was called.
                          type: string
                        response:
                          description: Response is the response from the function
                            call.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - name
                      type: object
                    text:
                      description: Text is the text content of the message part.
                      type: string
                    thought:
                      description: Thought indicates if this part is a thought.
                      type: boolean
                    thoughtSignature:
                      description: ThoughtSignature is the signature of the thought.
                      format: byte
                      type: string
                  type: object
                type: array
              role:
                description: Role is the role of the message (e.g. user, model, system,
                  function).
                type: string
              sessionId:
                description: SessionID is the ID of the ChatSession this message belongs
                  to.
                type: string
              timestamp:
                description: Timestamp is the time the message was created.
                format: date-time
                type: string
            required:
            - role
            - sessionId
            type: object
        type: object
    served: true
    storage: true

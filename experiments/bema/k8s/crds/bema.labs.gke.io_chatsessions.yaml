---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: chatsessions.bema.labs.gke.io
spec:
  group: bema.labs.gke.io
  names:
    kind: ChatSession
    listKind: ChatSessionList
    plural: chatsessions
    singular: chatsession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChatSession is the Schema for the chatsessions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChatSessionSpec defines the desired state of ChatSession
            properties:
              config:
                description: Config is the session configuration.
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: ChatSessionStatus defines the observed state of ChatSession
            properties:
              status:
                description: Status is the status of the session.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

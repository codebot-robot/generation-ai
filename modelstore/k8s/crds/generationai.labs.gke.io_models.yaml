---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: models.generationai.labs.gke.io
spec:
  group: generationai.labs.gke.io
  names:
    kind: Model
    listKind: ModelList
    plural: models
    singular: model
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Model is the Schema for the models API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec defines the desired state of Model
            properties:
              files:
                description: Files is a list of files that make up the model.
                items:
                  properties:
                    path:
                      description: Path is the relative path of the file within the
                        model.
                      type: string
                    sha256:
                      description: SHA256 is the SHA256 hash of the file content.
                      type: string
                  required:
                  - path
                  - sha256
                  type: object
                type: array
            type: object
          status:
            description: ModelStatus defines the observed state of Model
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
